.home .hero img {
    width: 600px;
    object-fit: cover;
}

blockquote {
    color: hsl(0, 0%, 30%);
}

main.page {
    word-break: break-all;
}

li.sidebar-sub-header a {
    color: hsl(0, 0%, 40%);
}